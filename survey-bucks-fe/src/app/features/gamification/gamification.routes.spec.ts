import { GamificationRoutes } from './gamification.routes';

describe('GamificationRoutes', () => {
  it('should create an instance', () => {
    expect(new GamificationRoutes()).toBeTruthy();
  });
});
